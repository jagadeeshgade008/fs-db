{
  "name": "sqlite",
  "version": "1.0.0",
  "description": "A simple example of using SQLite with Node.js",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3"
  }
}